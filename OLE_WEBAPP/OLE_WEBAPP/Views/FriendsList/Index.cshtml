@model IEnumerable<OLE_WEBAPP.Models.FriendsList>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Account1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Account2)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FriendshipTimeStamp)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Account1.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Account2.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FriendshipTimeStamp)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FriendshipId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FriendshipId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FriendshipId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
