@{
    ViewData["Title"] = "Existing Posts";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"]</title>
    <style>
        body {
            background-image: url('/images/generated-7566256_1280.jpeg');
            background-size: cover; /* Adjust the image size to cover the entire body */
            /* Add any other background properties here */
            color: #8D4004;
            font-family: 'Creepster';
        }

        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
        }

        .post-box {
            background-color: rgba(0, 0, 0, 0.7); /* Semi-transparent black background */
            border-radius: 8px;
            padding: 20px;
            margin-bottom: 20px;
            color: lightgray;
        }

        .post-text {
            color: white; /* White text color */
            font-family: 'Creepster';
            font-size: 18px;
            line-height: 1.6;
        }

        .btn {
            padding: 10px 20px;
            background-color: #8b0000; /* Rustic red color for button */
            color: #fff;
            text-decoration: none;
            border-radius: 5px;
            display: inline-block;
        }

            .btn:hover {
                background-color: #4b0000; /* Darker red color on hover */
            }
    </style>
</head>
<body>
    <div class="container">
        <h1>Existing Posts</h1>
        <div class="post-box">
            @*@if (Model.CommunicationRecord != null && Model.CommunicationRecord.Any())
            {
                foreach (var post in Model.CommunicationRecord)
                {
                    if (post != null) // Check if post is not null
                    {
                        <div class="post-container">
                            @foreach (var prop in post.GetType().GetProperties())
                            {
                                if (prop.Name != "Attachments" && prop.Name != "MessageID")
                                {
                                    <div class="post-property">
                                        <strong>@prop.Name:</strong> @prop.GetValue(post) <br />
                                    </div>
                                }
                            }
                            <div class="post-body">
                                <p>@post.MessageBody</p>
                                 Add code to display attachments if applicable 
                            </div>
                        </div>
                    }
                }
            }
            else
            {
                <p>No existing posts found.</p>
            }*@
            There are no existing posts.
        </div>
        <a href="/" class="btn">Back to Home</a>
    </div>
</body>
</html>